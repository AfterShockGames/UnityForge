namespace Forgery.Config
{
    /// <summary>
    ///     These codes can be used to inject runtime values into the forgery config
    /// </summary>
    public enum ConfigCode
    {
        /// <summary>
        ///     Gets the current directory as value
        /// </summary>
        CurrentDirectory
    }
}
